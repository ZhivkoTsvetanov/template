# get supported Locator strategy from core/elements_loader.py
login_username:
    locator: xpath
    selector: //input[@name="username"]
login_password:
    locator: xpath
    selector: //input[@name="password"]
